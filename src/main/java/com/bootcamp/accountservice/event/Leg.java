package com.bootcamp.accountservice.event;

/**
 * Lado de una pata de transferencia entre monederos yanki (Entrega 2, D8 Fase III): OUT es la
 * cuenta origen (retiro), IN es la cuenta destino (deposito).
 */
public enum Leg {
    OUT,
    IN
}
